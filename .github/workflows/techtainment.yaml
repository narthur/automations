name: Beeminder Exercise >> Techtainment
on:
  push:
  schedule:
    - cron: '0 * * * *' # hourly
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: apt-get update && apt-get install jq
      - run: ./scripts/techtainment.sh